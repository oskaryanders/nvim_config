-- catppuccin for theme control
-- https://github.com/kkoomen/catppuccin/nvim
-- the theme is manipulated in ui.theme

return {
	"catppuccin/nvim",
	name = "catppuccin", --sets the name for the plugin manager.
	lazy = false, --Loads the plugin automatically.
	priority = 1000, --Sets the load order (high=first load).
}
